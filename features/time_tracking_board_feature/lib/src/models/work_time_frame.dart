import 'package:domain/domain.dart';

class Work extends PeriodTimeFrame {
  const Work({
    required super.current,
    required super.previous,
  });
}

class Play extends PeriodTimeFrame {
  const Play({
    required super.current,
    required super.previous,
  });
}

class Study extends PeriodTimeFrame {
  const Study({
    required super.current,
    required super.previous,
  });
}

class Exercise extends PeriodTimeFrame {
  const Exercise({
    required super.current,
    required super.previous,
  });
}

class Social extends PeriodTimeFrame {
  const Social({
    required super.current,
    required super.previous,
  });
}

class SelfCare extends PeriodTimeFrame {
  const SelfCare({
    required super.current,
    required super.previous,
  });
}
